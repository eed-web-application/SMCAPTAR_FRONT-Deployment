apiVersion: v1
kind: Service
metadata:
  name: smartcaptar-front-service
spec:
  selector:
    app: smartcaptar-front
  ports:
    - name: http
      protocol: TCP
      port: 80
      "GET / HTTP/1.1" 304 0 "-"
      
      

